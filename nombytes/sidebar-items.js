window.SIDEBAR_ITEMS = {"enum":[["RangeType","An enum that can hold all the `Range*` types without being generic/trait based. We need this type because `SliceIndex<T>` is implemented for the individual `Range*` types rather than for the `RangeBounds<T>` trait that we could’ve otherwise used."]],"struct":[["NomBytes","A wrapper around [`bytes::Bytes`] to be able to use it with [`nom`]."]]};